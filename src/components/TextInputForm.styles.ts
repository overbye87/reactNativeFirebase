import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  сontainer: {
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
  },
  input: {
    flexGrow: 1,
    borderWidth: 1,
    borderColor: 'gray',
    paddingHorizontal: 20,
    marginVertical: 10,
    borderRadius: 5,
    marginRight: 20,
  },
});
