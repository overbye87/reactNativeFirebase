import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  сontainer: {
    width: 300,
  },
  input: {
    borderWidth: 1,
    borderColor: 'gray',
    paddingHorizontal: 20,
    marginVertical: 10,
    borderRadius: 5,
  },
});
